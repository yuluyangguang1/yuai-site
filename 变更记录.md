# 变更记录（强制）

> **规范（交接硬性要求）**：本项目每一次前端 / UI 修改（无论大小），都必须在本文件**顶部**追加一条记录。
> 格式见下方模板。下一个接手的人靠它还原演进脉络、避免重复劳动。
> 历史演进的系统性总结见 `交接报告.md`，本文件只记"步骤级"改动。

## 记录模板
```
## YYYY-MM-DD · 第 N 次 · 一句话标题
- 类型：优化 / 修复 / 新增 / 重构 / 文档
- 做了什么：
- 改动文件：
- 验证：
- 风险 / 遗留：
```


---

## 2026-09-23 · 第 26 次 · 新增「换尿布」「疫苗」模块 + 喂养左右侧轮换与统计
- 类型：新增
- 做了什么：
  - **换尿布模块（diaper.js）**：尿/便/混合三个一键大按钮即记当前时间；今日小结（共换 N 次/有尿/有便 + 上次时间）；最近记录列表 + 撤销删除。针对新生儿单手操作场景，不做表单。
  - **疫苗模块（vaccine.js）**：内置国家免疫规划 22 剂时间表，按宝宝生日（与「生长」共用 settings.baby.birthDate）自动推算每剂应种日期；接种进度条；行内「已接种」打卡/取消；将到期（≤7 天）高亮、逾期警示色。
  - **喂养增强**：母乳记录新增左/右/双侧字段（仅母乳显示）；页顶「今日概览」统计卡（今日次数/总量/距上次）+ 换边建议（上次喂 X 侧，这次建议先喂对侧）。
  - **提醒引擎第 7 条规则**：疫苗 ≤7 天到期或逾期 ≤30 天未种时首页提醒卡提示。
  - 首页：磁贴 8 → 10（尿布、疫苗），今日概览加「今日尿布」；日历索引聚合尿布记录与疫苗接种日；设置页统计自动纳入新模块。
  - 配色：新增 diaper（柔杏黄 #D0A94E）/ vaccine（静谧蓝 #6FA0C0）明暗双套 accent；新增 quick-btn 大按钮组件样式、vac-item 状态行样式。
- 改动文件：web/js/modules/diaper.js（新）、web/js/modules/vaccine.js（新）、web/js/modules/feeding.js、web/js/modules/home.js、web/js/reminders.js、web/js/util.js、web/js/storage.js、web/js/icons.js、web/css/styles.css、web/index.html
- 验证：node --check 全部通过；bugcheck 回归 47 → 53 项全部 PASS（新增：尿布三键/写入/日历索引、疫苗排期 22 剂/打卡/提醒触发、喂养侧别选择器）。
- 风险 / 遗留：疫苗时间表按月龄×30.44 天取整推算，与实际门诊安排可能有几天偏差，页内已注明「仅供参考」。自费（非免疫规划）疫苗未内置。
- 事故记录：编辑 bugcheck.js 时曾用 PowerShell GBK 读 + UTF8 写导致中文乱码且部分字节不可逆，已整体重写该文件（教训：**绝不用 PowerShell 读写含中文的源文件**，一律用编辑器工具）。

---

---

## 2026-09-20 · 推送记录 · 数据层加固 + 本地提醒引擎 已上线
- mom-assistant 仓库：`0f5f6e8`（数据层加固）+ `20017d2`（本地提醒引擎）推送成功（`f2e2363..20017d2`）。
- yuai-site 仓库：远端被自动 sync 机器人（`sync: refresh free offers` 等）推进导致分叉，fetch + rebase 重放后推送成功（`0ba5a29` 数据层加固 + `0e31cd3` 提醒引擎）。**注意：yuai-site 有自动提交进程活跃，以后推送若被拒，先 `git fetch origin main && git rebase origin/main` 再推。**
- 线上验证：两站 `storage.js` 均含 `DATA_VERSION`、`reminders.js` 均 HTTP 200（yuai-site 经 yuai-r.cn 重定向）。

---

## 2026-09-20 · 第 25 次 · 新增：本地提醒引擎（首页提醒卡，纯本地零网络）
- 类型：新增（后端功能 / 提醒引擎）
- 背景：用户要求「本地提醒」。纯本地应用不能用系统推送（无网络、无 Service Worker、file:// 环境），故做成**页面内提醒卡**——基于「当前时间 × 已有数据」在首页顶部显示该注意的事，点击直达对应模块。
- 做了什么：
  1. **新建 `js/reminders.js`**（提醒引擎，纯函数可单测）：`Reminders.evaluate(d, now)` 评估 6 条规则，返回触发的提醒数组。规则：
     - 喂养间隔 >3h（`feeding`）
     - 睡眠计时进行中（`openSleep` 存在）
     - 宫缩记录进行中（`openContraction` 存在）
     - 经期临近（按最近 ≤4 个间隔的平均周期预测，≤2 天提醒；异常周期 15~60 天外不预测避免误导）
     - 待产包告急（预产期 ≤30 天且完成度 <100%，或未开始）
     - 备份提醒（已有数据且 >7 天未导出，并入提醒卡与启动 toast 互补）
  2. **首页提醒卡**：`home.js` 在天空下方、「今日概览」上方渲染 `.remind-card`（有提醒才渲染，无提醒不占位）；每行可点击直达模块（复用 `[data-go]` 既有绑定，零新增 JS 事件）；左侧图标/边条按来源模块分色（复用 `[data-accent]`）。
  3. **挂脚本**：`index.html` 在 `home.js` 之前引入 `reminders.js`（依赖顺序）。
  4. **CSS**：`.remind-card` 左侧 3px 警示色边条 + `.remind-list` 可点击行（`:active scale(.98)`）。
- 改动文件：`web/js/reminders.js`（新建）、`web/js/modules/home.js`、`web/index.html`、`web/css/styles.css`、`（回归）bugcheck.js`（+9 断言）
- 验证：`bugcheck.js` 回归 **47 项全 PASS、0 失败 0 报错**（新增：喂养>3h触发/<3h不触发、睡眠计时中、待产包告急、空数据无提醒、首页提醒卡渲染、点击跳转模块）；`node --check` reminders.js / home.js 语法通过。
- 风险 / 遗留：
  1. 提醒只在「打开应用/切回首页」时评估（无后台推送），这是纯本地的固有限制，已在文案里避免「闹钟」类承诺。
  2. 经期预测基于平均周期，仅作参考（文案已用「可能/估算」措辞）。

---

## 2026-09-20 · 第 24 次 · 后端（数据层）加固：损坏自愈 + 导入清洗 + 版本迁移 + 备份提醒 + 数据统计
- 类型：新增 / 重构（数据层韧性，纯本地「后端」）
- 背景：项目是「纯本地、localStorage-only」定位，数据层就是它的「后端」。审计发现 5 个红线级缺口：① 损坏即丢数据（解析失败直接丢弃原文）；② save() 配额满时静默失败；③ 导入只查 object 不查结构，坏文件能搞坏应用；④ 无备份提醒（清浏览器即丢全部）；⑤ version 字段存在但从未用于迁移。
- 做了什么（B1→B5）：
  1. **损坏自愈**：`Storage.load()` 解析失败时，先把损坏原文备份到 `momhelper_data_v1_corrupt_backup` key 再回退默认值，用户可导出找回，绝不静默丢数据；标记 `_corruptRecovered`。
  2. **保存告警**：`save()` 配额满/异常时弹 `Util.toast` 错误提示（「存储空间不足，请导出备份后清理旧记录」）并返回 false（此前静默失败）。
  3. **导入清洗**：新增 `sanitize()`——settings 浅合并只留已知键的合法类型；5 个数组字段必须是数组且逐项是对象（null/字符串条目过滤）；4 个嵌套对象必须形状合法；非法字段回退默认。**导入坏文件不再可能搞坏应用**。
  4. **版本迁移脚手架**：`DATA_VERSION = 2`，`migrate()` 按版本号逐步升级（当前 v1→v2 补 `settings.lastExportISO`）；将来改结构只需追加 `if (v < N)` 步骤。
  5. **备份提醒 + 数据统计**：导出时记录 `settings.lastExportISO`；`app.js` 新增 `checkBackupReminder()`——已有数据且 >7 天未导出（或从未导出）时，启动后温和 toast 一次（带「去备份」直达设置）；`Storage.hasData()/stats()` 新 API；设置页新增「数据统计」卡（8 模块记录数，复用 `.kv` 分色）与「上次备份时间」提示。
- 改动文件：`web/js/storage.js`（重写）、`web/js/app.js`（exportAll 记录时间 + checkBackupReminder）、`web/js/modules/settings.js`（统计卡 + 备份提示）、`（回归）bugcheck.js`（+7 断言 + 内存版 localStorage 打桩 + URL.createObjectURL 打桩）
- 验证：`bugcheck.js` 回归 **38 项全 PASS、0 失败 0 报错**（新增：导入清洗×3、损坏自愈×2、版本迁移、导出记录时间、统计）；`node --check` 三个改动文件语法通过。注：jsdom fromFile 是 opaque origin 无 localStorage，已在测试里打桩内存版。
- 风险 / 遗留：
  1. 备份提醒每次启动最多一次，但用户首次安装无数据时绝不打扰（hasData 守卫）。
  2. localStorage 配额满时只告警不自动清理；将来可加「清理 N 天前旧记录」的主动工具。
  3. `_corruptRecovered` 标记暂未在 UI 展示，可后续在设置页提示「检测到过数据损坏并已从备份恢复」。

---

## 2026-09-20 · 第 23 次 · 部署：v3 全部改动推送 GitHub 并上线（mom-assistant + yuai-site/mom 双仓库）
- 类型：文档 / 部署
- 做了什么：把第 16~22 次的全部改动（v3 奶油柔彩、mobile-native、间距统一、卡片空白修复）推送到 GitHub 并确认线上生效：
  1. **mom-assistant**（独立仓库，应用在根目录）：commit `f2e2363`，`b419670..f2e2363 main`。克隆目录保留在 `C:\Users\高\Desktop\mom-assistant-repo`（后续改动可直接在此提交）。
  2. **yuai-site**（大仓库，应用在 `mom/` 子目录）：commit `edf4e2a`，`35d7575..edf4e2a main`。克隆目录 `C:\Users\高\Desktop\yuai-repo`。
- 线上验证：`https://yuluyangguang1.github.io/mom-assistant/css/styles.css` 已返回 v3 文件头（build 20260920-01），Pages 自动部署成功。
- 过程要点（网络环境记录）：本机到 github.com 主站不稳定（curl 000），但 codeload.github.com（zip 下载）稳定。采用「codeload 下载 zip → git init 还原 → fetch 接上远端历史 → push」的通道完成推送；mom/ 同步时先 `git reset origin/main` 再 `git add mom/`，避免把 zip 导出的行尾噪音（linger/novelweave/PDF 伪差异）带进提交。
- 改动文件：无前端文件改动（纯部署）。
- 验证：两个仓库 push 成功；线上 CSS 已是 v3。
- 风险 / 遗留：
  1. yuai-site 站点内 mom 页面的线上地址待用户确认（可能是 `https://yuluyangguang1.github.io/yuai-site/mom/` 或站点导航内路径），手机实际访问一次确认。
  2. GitHub Pages 部署有 1~3 分钟延迟，刚推完立即访问可能仍是旧版缓存，强刷即可。

---

## 2026-09-20 · 第 22 次 · 优化：间距系统 token 化 + 各项宽度统一 + 超窄屏适配
- 类型：优化（间距系统 / 宽度统一 / 移动端适配）
- 做了什么：
  1. **间距 token 系统**：新增 `--space-xs(6px)/sm(10px)/md(14px)/lg(20px)` 四档，全站间距统一收口——以后改间距只动这 4 个变量，不再满文件找硬编码。
  2. **间距统一**（全部替换为 token）：卡片 `padding/margin-bottom`、`.kv li`（8→10px，与 `.list li` 对齐）、`.seg`、`.warn-box`、`label`、`.btn` 行距、`.modal-actions`、`.quick-grid/.mod-grid/.stat-row/.record-grid` 的 gap。
  3. **宽度统一**：
     - `.seg-btn` 加 `flex: 1 1 0; justify-content: center`——「母乳/奶瓶/奶粉/辅食」等分段选择器变为**等宽**铺满整行（原生 App 类型选择器的标准形态），不再按文字长短参差。
     - `.mod-head` 去掉 `2px` 侧偏移（`margin: 2px 2px 14px` → `0 0 var(--space-md)`），页头与卡片左边缘**严格对齐**。
  4. **超窄屏适配（<360px：iPhone SE 一代/折叠屏外屏/分屏半屏）**：新增 `@media (max-width: 359px)`——`.view` 内边距 18/16→14/12px、卡片 20→16px、天空 hero 184→160px（负 margin 同步 14/12，保住 `--sat` 联动不断痕）、磁贴内边距 16→13px、`stat-row/mod-grid` 固定 2 列、分段按钮 14→13px 字号、日历翻页钮 34→32px。常规手机（≥360px）布局零变化。
- 改动文件：`web/css/styles.css`（token 新增 + 约 14 处间距/宽度替换 + 新增超窄屏 media）
- 验证：`bugcheck.js` 回归 **31 项全 PASS、0 失败 0 报错**；CSS 括号 273/273 平衡；`margin-bottom: 14px` / `gap: 10px` 硬编码清零（全部 token 化）。
- 风险 / 遗留：超窄屏的 `.home-sky` 负 margin 已与 `.view` 内边距同步（14px/12px），但真机需确认刘海机型顶部不断痕（`--sat` 联动的老规矩）。

---

## 2026-09-20 · 第 21 次 · 优化：mobile-native 五件套，让网页在任何手机/浏览器上感觉像原生 App
- 类型：优化（移动端原生体验 / 基于 emilkowalski/skills 的 mobile-native 规范）
- 做了什么（逐项对照 mobile-native 规范补齐缺口）：
  1. **输入框字体 15px → 16px**：iOS Safari 聚焦 <16px 输入框时**自动缩放页面**，打断记录流程。16px 是免缩放阈值，喂养/睡眠/宫缩等所有表单输入框受益。
  2. **`-webkit-text-size-adjust: 100%` + `text-size-adjust: 100%`**：禁止 iOS 横屏时文字自动放大（布局错乱源头），奶油风排版在任何方向下都稳定。
  3. **`touch-action: manipulation`**：取消按钮/链接点击的 300ms 延迟（旧浏览器双击缩放判定），所有可点元素（按钮/磁贴/Tab/列表项）点击即响应，更像原生 App 的即时反馈。
  4. **theme-color `#f6f1ec` → `#FDF9F4`**：v2 玻璃暖房的旧色改为 v3 奶油白，Android 状态栏 / iOS 添加到主屏的启动画面与奶油风一致。
  5. **状态栏样式 `default` → `black-translucent`**：iOS 添加到主屏后，状态栏透明融入奶油背景（而非白底割裂），配合 `viewport-fit=cover` 实现真全屏。
  6. **manifest.webmanifest 同步**：`background_color` / `theme_color` 改为 `#FDF9F4`。
- 改动文件：`web/index.html`（theme-color / 状态栏样式）、`web/css/styles.css`（body 三件套 + 输入框 16px）、`web/manifest.webmanifest`（主题色同步）
- 验证：`bugcheck.js` 回归 **31 项全 PASS、0 失败 0 报错**；CSS 括号 263/263 平衡。
- 风险 / 遗留：
  1. `user-scalable=no` + `maximum-scale=1.0` 已禁用双指缩放（防误触），但**牺牲了可访问性**（视力障碍用户无法放大）。若将来收到相关反馈，可移除这两条，仅靠 `font-size:16px` 防输入框缩放。
  2. `black-translucent` 状态栏在浅色奶油底下，状态栏图标（时间/电量）是白色，需真机确认对比度；若看不清，改回 `default` 即可。

---

## 2026-09-20 · 第 20 次 · 优化：奶油风微动效三件套（分段弹跳 + 列表错落淡入 + 磁贴按压下沉）
- 类型：优化（动效细节 / 基于 emilkowalski/skills 规范）
- 做了什么（三处，均只动 CSS，零 JS 改动）：
  1. **分段控件激活弹跳**：`.seg-btn.active .ico` 新增 `segPop` 动画（`scale(.8)→(1.15)→(1)`，spring 曲线），切换「母乳/奶瓶/奶粉/辅食」等类型时图标轻微弹跳，给用户明确的「已选中」确认感，契合奶油风的 Q 弹定位。
  2. **记录列表错落淡入**：`.list li` 新增 `listIn` 动画（`translateX(-8px)` 从左滑入 + 淡入，前 3 项延迟 0/40/80ms、第 4 项起 120ms），模块页核心内容（喂养/睡眠/宫缩等记录列表）进入时比整页 `.view-enter` 更细腻、有层次。
  3. **首页磁贴按压下沉**：`.record-tile:active` 从 `scale(.97)` 改为 `scale(.96) translateY(1px)`，按压时轻微下沉 + 缩小，更像「按进奶油里」的 Q 弹触感（emilkowalski/skills：按压反馈该用 spring + 位移，而非单纯缩放）。
- 参考规范：emilkowalski/skills（Vercel/Linear 背景 UI/动效规范集）——进入动画用 ease-out（`.list li` 用 `--ease`）、按压/确认用 spring（`.seg-btn.active` / `.record-tile:active` 用 `--spring`）、动效 < 400ms（均 .3s~.35s）。
- 改动文件：`web/css/styles.css`（新增 `segPop` / `listIn` 两个 keyframes + 三处动画规则）
- 验证：`bugcheck.js` 回归 **31 项全 PASS、0 失败 0 报错**；CSS 括号 263/263 平衡。
- 风险 / 遗留：无。所有动画均在 `@media (prefers-reduced-motion: reduce)` 下自动禁用（全局 `*{animation:none!important}` 覆盖）。

---

## 2026-09-20 · 第 19 次 · 修复：卡片「上半部分空白」——移除玻璃时代残留（isolation + 折射层 + feeding 空状态插画）
- 类型：修复（视觉 bug / 玻璃时代残留清理）
- 背景：用户反馈「每个模块上有很大的空白框，卡片上半部分空白、内容被挤到下半部分」。系统排查定位到三处玻璃暖房时代的残留，在奶油风实色卡片下会暴露为空白块：
  1. **`.card { isolation: isolate }` + `.card > * { position: relative; z-index: 1 }`**——v2 玻璃暖房为了把内容抬到折射层之上而加，奶油风卡片是实色、无折射层，这两条规则反而导致 `.card` 建立独立堆叠上下文，在某些渲染引擎下让标题（h2）与后续内容（.seg / 表单）之间产生异常层叠间隙，表现为「上半部分空白」。
  2. **`.grefract` 折射层**——glass.js 在非首页会 `clearAll()`，但 CSS 里 `.grefract { opacity: .82; filter: url(#liquidRefract) }` 仍定义着；若因缓存/时序问题折射层被注入，奶油风实色卡片上半部分会出现一块半透明扭曲白块。v3 卡片不透底，折射已无意义。
  3. **feeding.js 空状态漏网**——#15 移除了 6 个模块的 `.empty-illo` + `illo('empty')`，feeding 是第 7 个漏的，仍渲染 `<div class="empty-illo">`（虽 CSS 隐藏了插画，但容器残留空间）。
- 修复：
  1. `.card` 移除 `isolation: isolate`；删除 `.card > * { position: relative; z-index: 1 }`（同时清理 `.record-tile/.stat/.modal/.mod-item` 的同类规则——它们也不需要）。
  2. `.grefract` 改为 `display: none !important`（强制禁用折射层，彻底排除变量）。
  3. `feeding.js` 空状态改为 `<p class="hint">暂无</p>`（与其他 6 个模块一致）。
- 参考：本次修复前研读了 emilkowalski/skills（Vercel/Linear 背景的 UI/动效规范集，39.3k star），确认「进入动画用 ease-out」「点击/按压用 spring」「半透明阴影 > 实色边框」「mobile-native 中和 sticky hover」等要点——我们的 spring 用于 :active 按压反馈、ease 用于过渡、shadow-card 半透明、@media(hover:none) 中和，均符合规范。
- 改动文件：`web/css/styles.css`（`.card` 去 isolation、`.card > *` 删除、`.grefract` 强制隐藏）、`web/js/modules/feeding.js`（空状态去插画）
- 验证：`bugcheck.js` 回归 **31 项全 PASS、0 失败 0 报错**；CSS 括号 250/250 平衡；`isolation` / `z-index: 1` 已全部移除（剩余 z-index 均为正常层叠：tabbar 10 / modal 100 / toast 1200）。
- 风险 / 遗留：无。若将来想恢复折射（奶油风下无意义），把 `.grefract` 的 `display: none !important` 改回原样式即可。

---

## 2026-09-20 · 第 18 次 · 修复：模块页顶栏始终实色，消除「标题上方 66px 透明空白带」
- 类型：修复（布局留白 / 视觉断裂）
- 背景：用户反馈「标题上还是有大片的空白」。根因：`.app-header` 是 `position:sticky` 透明顶栏（高 ≈66px），原本为首页天空 hero 服务；但**模块页没有天空**，透明顶栏让「页头标题（.mod-head）」上方露出一条 66px 的透明空白带，且内容滚动时会从顶栏下方穿过造成重叠。
- 修复：`app.js` 的 `syncHeader()` 改为分场景策略——
  - **首页（天空 hero）**：保持原逻辑，默认透明，滚动 >8px 后补实色底；
  - **非首页（模块/日历/设置）**：**始终实色**（`is-scrolled` 常驻），顶栏与卡片同源（奶油白 `--surface`），视觉一体，空白带消除。
- 同步更新 `bugcheck.js`：删除过时断言「初始顶栏为透明态」（原逻辑），新增两条符合新策略的断言——「非首页顶栏始终实色」（日历页验证）、「首页初始顶栏为透明态」（切回首页验证）。
- 改动文件：`web/js/app.js`（`syncHeader()` 分场景）、`（回归）bugcheck.js`（2 条断言更新）
- 验证：`bugcheck.js` 回归 **31 项全 PASS、0 失败 0 报错**；`node --check` 校验 app.js 语法通过。
- 风险 / 遗留：无。首页天空 hero 的透明顶栏 + 滚动玻璃化体验完整保留。

---

## 2026-09-20 · 第 17 次 · 重构：设计系统 v3「奶油柔彩」——推翻玻璃暖房，整体重写 styles.css
- 类型：重构（设计系统换代）
- 做了什么：
  1. **设计方向**：按用户选择，推翻 v2「玻璃暖房」（磨砂玻璃 + 晨雾紫 + backdrop-filter），转向 **v3「奶油柔彩」（Creamy Soft）**——奶油白底 + 大圆角实色柔彩卡片 + 圆润字体 + 弹性微动效，更现代亲和的主流母婴高级感。
  2. **核心变化**：
     - 底色：柔彩渐变 → 温暖奶油白（`#FDF9F4 → #F8F2EC`）
     - 卡片：半透明磨砂玻璃 → 实色奶油白（`#FFFFFF`）+ 大圆角（24px）+ 柔和大半径投影，**完全去掉 backdrop-filter 依赖**
     - 主色：晨雾紫 `#6457B5` → 蜜桃珊瑚 `#E8907C`（白字对比 ≈4.8，AA）
     - 磁贴：统一白卡 + 分色细条 → **每模块一张淡色身份卡**（`--tile-weak` 实底，`--tile` 图标/角标），柔彩感拉满
     - 动效：普通 ease → 弹性 `cubic-bezier(.34,1.56,.64,1)`（Q 弹奶油感），仍 < 400ms
     - 圆角：全面加大（14/18/22 → 16/20/24）
  3. **保持不变**：所有 class 名、JS 逻辑、明暗双模式、模块 8 分色 [data-accent] 体系、WCAG AA 对比度、prefers-reduced-motion 降级、折射层机制（glass.js 首页注入 .grefract）、--sat 刘海安全区联动。
  4. **工程方式**：class 名全保留、JS 零改动，只重写 `styles.css`（731 行 → 730 行，253/253 括号平衡）。先备份 `styles.css.v2.bak`，验证通过后删除。
- 改动文件：`web/css/styles.css`（整体重写）
- 验证：`bugcheck.js` 回归 **30 项全 PASS、0 失败 0 报错**；CSS 括号 253/253 平衡；JS 用到的 85 个 class 全部有定义（补回 `.row-switch`）；引号成对 0 异常。
- 风险 / 遗留：
  1. 新主色蜜桃珊瑚在真机上的观感（尤其与 sky.js 程序化天空的配色协调）需用户确认——天空是程序调色板，与奶油底的衔接靠 `--sky-fade`，可能需微调。
  2. 模块分色 8 组从「玻璃暖房」的低饱和调到「奶油柔彩」的更亮更奶，真机对比度（尤其 `--tile-weak` 淡底上的 `--tile` 文字/图标）建议抽查经期/孕期两组最浅的。
  3. 折射层（glass.js）在奶油风下仍有效但视觉比重降低（卡片不再透明），设置页「真实玻璃折射」开关保留，用户可自决。

---

## 2026-09-20 · 第 16 次 · 重构优化：问候语融合天空 + Tab 激活态 + 模块页身份页头
- 类型：重构 / 优化（视觉 + 信息架构）
- 做了什么（三处，均只动 CSS 与 app.js 一行注入，零破坏）：
  1. **问候语与天空融合**：`.home-greet` 由「天空中部深色补丁式气泡」改为「落到底部的轻融合式排版」——位置从 `top:50%` 改为 `bottom:14px`，背景/边框/阴影全面调轻（`.28→.18` 底、`.22→.14` 边），同时 `sky-veil` 下缘渐变提前加深兜住文字对比度。消除问候语气泡的「补丁感」，与天空一体。
  2. **Tab 激活态优化**：`.tab.active` 移除 `transform: scale(1.04)`（缩放会让相邻 tab 轻微跳动、观感粗糙），改为顶部 4px 小圆点指示（`.tab.active::after`）+ 文字加重 + 主色弱底，表达更克制。
  3. **模块页身份页头**：`app.js` 的 `showTab()` 在记录类模块页（非首页/日历/设置）自动注入 `.mod-head`（分色图标圆底 + 模块标题），消除「进模块直接是卡片、无身份」的空白感（变更记录 #15 遗留项 D）。图标/标题复用 `[data-accent]` 模块分色，与首页磁贴、列表左边条同源。设置页已有 `.page-head`、首页有天空问候、日历有 `.cal-head`，均不重复注入。
- 改动文件：`web/css/styles.css`（`.home-greet`、`.sky-veil`、`.tab.active`、新增 `.mod-head`）、`web/js/app.js`（`showTab()` 内注入页头，约 8 行）
- 验证：`bugcheck.js` 回归 **30 项全 PASS、0 失败 0 报错**；`node --check` 校验 app.js 语法通过；jsdom 实测确认 feeding 页注入 `.mod-head`（标题「喂养」）、settings/home 页不注入。
- 风险 / 遗留：
  1. `.mod-head` 图标用 `icon(id)`，依赖 `icons.js` 中对应图标存在（8 个记录模块均有）；若将来新增模块缺图标，页头会显示空圆底，需在 icons.js 补图标。
  2. 问候语落到底部后，天空上半部分（约 120px）为纯天空画面，这是设计意图（呼吸感）；若觉得仍空，可再压 `.home-sky` 高度。

---

## 2026-09-20 · 第 15 次 · 排查并修复：全站"空白块"——移除空状态大光环/大插画（主源）
- 类型：修复（布局留白 / 可读性）
- 背景：用户反馈"页面上的空白块非常多"。做全站系统排查，定位空白来源：
  - A 透明顶栏留白（每页顶部 56px，中间全空，为首页天空让路，模块页显空）
  - **B 空状态大光环/大插画 `.empty-illo`：152px 直径光环 + 104px 插画，几乎全空** —— 全新安装下散布 7~8 处（睡眠/生长/经期×2/孕期/待产包/记账），是"非常多空白块"的主源
  - C 首页天空 184px（已由 312px 收紧，第 14 次）
  - D 模块页顶部无标题（进模块直接是卡片，无身份，显空）
- 本次修复（主源 B）：
  - CSS：`.empty-illo` 整块重写——移除 `::before` 152px 光环、`.illo` 104px 插画，改为安全兜底（若有引用折叠为左对齐小提示、隐藏光环与插画）。
  - 6 个模块移除空状态插画：sleep / growth / period(×2) / pregnancy / packing / finance，空状态改为左对齐克制文字提示（`<p class="hint">`）。
  - packing 顶部"按场景勾选…"原本**无论有无数据都**显示大插画，已改为纯文字提示。
  - 契合用户"纯文本、左对齐、无图标"的 UI 偏好。
- 改动文件：css/styles.css、js/modules/{sleep,growth,period,pregnancy,packing,finance}.js
- 验证：`bugcheck.js` 回归 **30 项全 PASS、0 失败 0 报错**（新增断言"空状态不再渲染 .empty-illo"）；7 个改动文件已同步 GitHub（mom-assistant、yuai-site/mom）。
- 风险/遗留：A（透明顶栏）、C（天空）、D（模块顶部无标题）为次要空白源，本次未动；如需进一步压缩顶栏留白或给模块页加纯文本标题填充身份，可在此基础上继续（见下方说明）。

## 2026-09-20 · 第 14 次 · 修复：压缩首页天空高度，消除顶部"非常大的一块空白"
- 类型：修复（布局留白 / 可读性）
- 做了什么：
  1. **根因**（用户真机反馈）：首页顶部 `.home-sky` 高度高达 **312px（宽屏 340px）**，而问候语 `.home-greet` 被 `position:absolute; bottom:18px` 钉在天空**最底部**，导致天空上半部分（约 250px）全空。当前时段（夜间）天空呈深蓝紫调，整片偏暗，看着就是"一大块空白"。叠加全站通用的**透明顶栏**（仅两侧头像/夜间按钮、中间空），各页顶部都显空。
  2. **压缩天空为紧凑 hero**：`.home-sky` 高度 `312px → 184px`（宽屏 `@media(min-width:680px)` 内 `340px → 200px`）；`margin-bottom` `18px → 14px`。
  3. **问候语垂直居中**：`.home-greet` 由 `bottom:18px` 改为 `top:50%; transform:translateY(-50%)`，问候语落在天空中部，上方不再留大片空天空。
  4. **静态渐变兜底**：`.home-sky` 新增 `background: linear-gradient(180deg,#4a4f7a,#6b6796)`，即使天空画布（sky.js）未加载也是一块有质感的色，而非空白块。
  5. 设计意图：保留用户要求的"顶部 swiftui-sky 每日天气背景"（sky.js 程序化天空仍渲染在 184px 区域内），只是从"占满首屏的高天空"改为"紧凑 hero"，呼吸感保留、空旷感消除。
- 改动文件：`css/styles.css`（`.home-sky`、`.home-greet`、宽屏 media 内 `.home-sky` 高度三处）
- 验证：`bugcheck.js` 回归 **29 项断言全 PASS、0 失败 0 报错**（含"首页天空 canvas 存在""首页问候 h1 存在"等）；改动仅 CSS 布局，未动 JS 逻辑。
- 风险 / 遗留：
  1. 顶栏为空是**全站通用透明设计**（首页需天空透出），模块页顶部因内容紧贴，无大空白；若用户仍觉得各模块页顶栏"空"，可后续在模块页给顶栏加一层淡玻璃底（滚动态已有，可改为常驻）。
  2. 真机观感（尤其窄屏 / 刘海机 `--sat` 下的对齐）需用户在本机确认一次。

## 2026-09-20 · 第 13 次 · 修复：移除影响可读性的"实验性折射"层（回退 #12/#11 的视觉失控部分）
- 类型：修复（可读性回归）
- 做了什么：
  1. **根因**（用户真机反馈）：#11/#12 加的折射在真机上失控——① `glass.js` 把 `.grefract` 折射 canvas 注入到**每一张 `.card`**，在模块页（无程序化天空、背景只是纯色渐变）被摘取后经受 `scale=11` 位移滤镜扭曲，在卡片顶部/边缘产生难看色带、压低对比；② `.card::after` 表面光层用了 `filter:url(#liquidSheet)`（`scale=16` 位移），把右上角柔光推到卡片别处，形成"不知名的白光斑"。两者叠加 = 用户看到的"每个模块上方那块不知名、却严重干扰阅读的东西"。jsdom 不渲染 SVG 滤镜，故此前回归无法暴露。
  2. **折射层收敛为"仅首页"**：`glass.js` 的 `Glass.wake()` 增加检测——仅当存在且连入 `#skyCanvas`（即首页）时才启用折射并注入 `.grefract`；切到任意模块页一律 `clearAll()` 清除折射层。折射本就是为"首页程序化天空"设计的，模块页无天空可折射，强行折射无意义且难看。
  3. **删除 `#liquidSheet` 位移表面光层**：移除 `css/styles.css` 中 `.card::after` 整条规则（含 `#liquidSheet` 滤镜引用）及其 opacity 覆盖；`index.html` 同步删除已废弃的 `#liquidSheet` 滤镜定义。
  4. **保留**：磨砂玻璃底 + 轻位移 rim 高光边（`#liquidRim`，scale 2.5，全浏览器可见、不碍阅读，是 liquid glass 标志性且安全的特征）。首页折射（设置-真实玻璃折射 开启时）仍保留。
  5. 回归脚本 `bugcheck.js`：删除作废的"#liquidSheet 滤镜存在"断言；新增"模块页不注入折射层(.grefract)"断言，把本次修复固化进测试。
- 改动文件：`js/glass.js`、`css/styles.css`、`index.html`、`（回归）C:/Users/高/.workbuddy/binaries/node/workspace/bugcheck.js`
- 验证：`node --check` 校验 glass.js/app.js/settings.js 语法通过；`bugcheck.js` 更新后 **29 项断言全 PASS、0 失败 0 报错**（含新增的"模块页不注入 .grefract"）。
- 风险 / 遗留：
  1. 首页折射（开启时）的视觉仍需在真浏览器（Chrome/Safari/Firefox 各开一次 web/index.html）确认；jsdom 不渲染 SVG 滤镜，本环境无法代验。
  2. 若希望连首页也彻底去掉折射（只要干净磨砂玻璃），把 `js/modules/settings.js` 里 `realRefraction` 默认值改为 `false` 即可，或直接关设置开关。

---

## 2026-09-20 · 第 12 次 · 全平台真折射（Chrome/Edge/Firefox/Safari 统一可见）
- 类型：优化（实验性，闭环之前的遗留）
- 做了什么：
  1. 新建 `js/glass.js`（Glass 模块）：用「canvas 镜像背景 + 普通 filter:url()」实现**真正的全平台**背景折射——之前 #11 的折射依赖 `backdrop-filter: url()`，只在 Safari/WebKit 生效，Chrome/Edge 不支持，所以苹果浏览器之外看不到"透过玻璃扭曲背景"。本模块把页面背景（body 渐变 + 首页 skyCanvas）精确重绘进离屏场景 canvas，给每个玻璃面注入 `<canvas class="grefract">` 摘取正后方背景并轻微放大，再施加 `#liquidRefract` 滤镜（模糊+位移）。普通 `filter:url()` 四大引擎全支持，故所有浏览器都能看到真实折射。
  2. 性能与降级：仅首页（有 skyCanvas）开常驻 rAF 循环；其余页面靠滚动/切页/缩放触发单帧刷新；`prefers-reduced-motion` 不跑循环；不支持 2D canvas 或 `filter:url()` 的环境自动禁用，回退磨砂玻璃，零报错。设置页新增「真实玻璃折射」开关（默认开，`Storage.settings.realRefraction`），低端机可关。
  3. `index.html` 新增 `#liquidRefract` 滤镜并挂载 `glass.js`；`app.js` 切页后调 `Glass.wake()`、切主题后调 `Glass.setTheme()`；`styles.css` 加 `.grefract` 样式与玻璃宿主 `isolation`/内容抬层，并**移除仅 Safari 的 `@supports` 折射块**（已无必要）。
- 改动文件：`js/glass.js`（新）、`index.html`、`js/app.js`、`js/modules/settings.js`、`css/styles.css`
- 验证：`bugcheck.js`（jsdom 真实加载整套前端）新增 2 条断言（#liquidRefract 滤镜存在、Glass 模块注册），合计 **29 项全 PASS、0 失败 0 报错**；`node --check` 校验 glass.js/app.js/settings.js 语法通过；CSS 完整性括号 260/260、引号成对。
- 风险 / 遗留：
  1. `feTurbulence`+`feDisplacementMap` 是逐像素滤镜，每个玻璃面一张折射 canvas，滚动重绘有合成成本；已在首页之外关掉常驻循环、仅刷视口内元素、dpr 限 2 缓解，但极低端安卓机仍可能轻微卡顿——已提供开关兜底。
  2. 折射层与卡片自身 `backdrop-filter` 磨砂叠加，读起来像更厚的液态玻璃而非重影（两者本就是同一场景的不同渲染）；opacity .82 经真机可再微调。
  3. jsdom 不渲染 SVG 滤镜，折射的视觉必须在真浏览器确认；本环境无沙箱外浏览器，请你在 Chrome/Safari/Firefox 各开一次 `web/index.html` 核对。

---

## 2026-09-20 · 第 11 次 · SVG 真折射液态玻璃效果（边缘弯折 + 卡片折射光 + Safari 真折射）
- 类型：优化（实验性）
- 做了什么：
  1. 注入 SVG 滤镜：在 `index.html` body 顶部加隐藏 `<svg class="glass-defs">`，含两个滤镜——
     - `#liquidRim`：`feTurbulence`（高频 fractalNoise）+ `feDisplacementMap`（scale 2.5），把玻璃边缘高光**弯折成液态反光**。
     - `#liquidSheet`：低频大波浪 + 模糊，用于"折射表面光"。
  2. 玻璃边缘液态弯折：给全部 rim 高光边（卡片/按钮/磁贴/底栏/头像/弹窗…）加 `filter: url(#liquidRim)`，原本笔直的 1px 高光边呈现柔和的液态弯折——liquid glass 最标志性可见特征，**全浏览器可用**（filter:url 对 HTML 元素被广泛支持）。
  3. 卡片折射表面光：`.card` 新增 `::after` 折射层（被 `#liquidSheet` 扭曲的柔光椭圆，`mix-blend-mode: screen`，置于右上避标题），模拟液体表面折射的环境反光；`.card` 加 `isolation: isolate` 并用 `.card > * { position:relative; z-index:1 }` 把内容抬到光层之上，**不影响可读性**。
  4. 厚度阴影：增强 `--glass-shadow` token，加一层宽而淡的投影（浅/暗双模式），模拟玻璃厚度。
  5. Safari 真折射增强：`@supports (-webkit-backdrop-filter: url(#liquidSheet))` 下，给支持的浏览器在模糊之上叠加**真正的背景折射**（Safari/WebKit）；Chrome/Edge 因不支持 backdrop-filter 引用 SVG 滤镜，自动忽略，保持原磨砂玻璃——**已诚实标注此技术边界**。
  6. `prefers-reduced-motion` 下降低折射光 opacity（折射层本就静态、不动 SVG 参数，避免重算开销）。
- 改动文件：`web/index.html`（加 SVG defs）、`web/css/styles.css`（rim 加 filter:url、卡片折射层、厚度阴影 token、@supports 真折射、reduced-motion）、`bugcheck.js`（新增 3 条断言锁滤镜存在）
- 验证：`bugcheck.js`（jsdom 真实加载）新增 3 条断言（#liquidRim / #liquidSheet / .glass-defs 存在），合计 **27 项全 PASS、0 失败 0 报错**；CSS 完整性括号 254/254、引号成对 0 异常。注意：jsdom 不渲染 SVG 滤镜，视觉效果需在真实浏览器（Safari 见真折射、Chrome 见弯折边+表面光）确认。
- 风险 / 遗留：① `feTurbulence`/`feDisplacementMap` 是逐像素滤镜，每张卡片的折射层 + 所有 rim 在**滚动重绘时**有合成成本，低端安卓机可能出现轻微滚动卡顿；如需彻底消除，可把 `.card::after` 折射层改为只在桌面端（`@media (min-width:980px)`）启用，或去掉 rim 的 filter。② "透过玻璃折射背景"在 Chrome/Edge 纯前端**无法实现**（浏览器限制），仅 Safari 经 @supports 实现真折射；若要在全平台看到真折射，需改用 WebGL/canvas 背景镜像方案（更重、离线页需自绘背景）。③ 折射光层 `screen` 提亮，深色背景（暗色模式）下对比已调，但极深场景仍建议人工目检。

---


- 类型：优化
- 做了什么：
  1. 模块页分色身份：在 `App.showTab` 给 8 个记录类模块页的 `#view` 打 `data-accent="<模块id>"`，首页/日历强制清除（避免串色）。CSS 复用既有全局 `[data-accent]` 柔彩色板，使——列表药丸的左边条（inset 阴影，hover 改边色不丢条）、列表行图标圆、卡片标题竖线——在喂养/睡眠/孕期等各页自动呈现对应模块柔彩色，与首页磁贴、日历圆点同一套色，视觉连续。
  2. 详情 `.kv` 关键值行玻璃化：记账/经期/孕期详情的 `ul.kv` 每行从"裸行+虚线"改为带极淡玻璃底、模块分色左边条的小卡，关键数字 `b` 按模块着色。
  3. 触屏适配补强：把 `.list li:hover` 纳入 `@media (hover:none)` 中和，避免手机上列表行卡在抬升态。
- 改动文件：`web/js/app.js`（showTab 加 data-accent 逻辑）、`web/css/styles.css`（列表 inset 边条、row-ico 分色、card h2 竖线分色、.kv 玻璃卡、hover:none 补强）
- 验证：`bugcheck.js`（jsdom 真实加载）新增 3 条断言（喂养页 #view 带 data-accent=feeding、首页/日历清除），合计 **24 项全 PASS、0 失败 0 报错**；`node --check` 校验 app.js 语法通过；CSS 完整性括号 247/247、引号成对 0 异常。
- 风险 / 遗留：分色边条用 inset 阴影实现，降级 `@supports not backdrop-filter` 下仍是实色卡片 + 实色边条，无事故。设置页（id=settings 不在色板）默认回退主色，符合预期。

---

## 2026-09-20 · 第 9 次 · 模块页记录列表玻璃药丸化 + 表单标题
- 类型：优化
- 做了什么：
  - **记录列表行升级为玻璃药丸卡片**：原各模块（喂养/睡眠/记账/经期/孕期/生长/待产包）的记录列表是「裸行 + 下边框」的朴素分隔线样式，与首页「玻璃暖房」语言差距最大、也是用户每天停留最久的界面。改为每一条独立成卡的磨砂玻璃药丸——半透明玻璃底 + 1px 描边 + 大圆角 + 玻璃阴影 / 顶部高光，条目间距 9px 形成卡片堆叠的呼吸感；悬停时整条轻微浮起并描上主色弱边。
  - **列表图标圆化**：喂养等带图标的行，`.row-ico` 的图标改为「柔彩圆底 + 主色图标」（与主色 `--primary-weak`/`--primary` 同套 token，与首页磁贴图标圆、日历模块色一致），左端给每条记录一个柔和的功能色锚点。
  - **表单字段标题优化**：全局 `label` 由 14px 常规正文改为 13px 半粗（`font-weight:600`）的紧凑字段标题，落在输入框正上方，读起来更像"带标题的字段"而非"一段说明文字"。
  - 纯 CSS 改动，未动任何 JS、未新增 DOM。
- 改动文件：`web/css/styles.css`（重写 `.list`/`.list li`/`.row-ico .ico` 区块；`label` 字重与字号微调）
- 验证：jsdom 回归新增 2 条断言（「模块页渲染出记录列表」「记录行含图标圆(row-ico)」），合计 **21 项全 PASS、0 失败 0 报错**；CSS 完整性校验通过（括号 246/246、引号成对 0 异常）。
- 风险 / 遗留：
  - 列表行从 `align-items:flex-start` 改为 `center`，多行文本行（如睡眠「开始时间<br>时长」）整体垂直居中、删除按钮随之居中，观感更平衡；若个别模块的多行行内文字期望顶部对齐，可针对该类加 `align-self:flex-start`。
  - 列表圆仅用了主色，**未按模块分色**（喂养=雾蓝、睡眠=藕紫）。要做到「每条记录按模块着色」，需给各模块列表 `<li>` 加 `data-accent="<模块id>"`（复用已上提的全局 `[data-accent]` 色板），属 JS 层小改动，本次未做，列为下一候选。
  - 列表圆仅用于带 `.row-ico` 的行（目前仅喂养）；其余模块行无图标，保持中性药丸 + 悬停主色删除按钮，已统一协调。

## 2026-09-20 · 第 8 次 · 空状态统一为「柔光环」
- 类型：优化
- 做了什么：各模块空状态原本是 96px 裸图标紧接一行「暂无」，视觉重心松散、观感粗糙，且 6 个模块（喂养/睡眠/生长/经期/孕期/待产包/宫缩/记账）之间还不一致（有的包在卡片里、有的裸放）。统一为**柔光环 + 轻浮插画 + 居中说明**：`.empty-illo::before` 用 152px 的 `radial-gradient`（极淡主色 `--primary-weak` 向外透明）做光晕，插画放大到 104px 并复用已有的 `float` 微浮动，说明文字居中与光环同一视觉重心。纯 CSS、不新增 DOM、不改任何 JS。
- 改动文件：`web/css/styles.css`（重写 `.empty-illo` 与 `.empty-illo .illo`，新增 `.empty-illo::before`、`.empty-illo + .hint`）
- 验证：jsdom 回归 **19 项断言全 PASS、0 失败 0 报错**；CSS 完整性校验通过（括号 245/245、引号成对 0 异常）。
- 风险 / 遗留：
  - 光环是绝对定位的 152px 圆，若将来有更窄的容器（<300px）可能轻微溢出，届时给 `.empty-illo::before` 加 `max-width:100%` 即可。
  - 只统一了"容器观感"，各模块空状态**文案**仍不一致（有的「暂无」、有的有引导语）。若要求文案也统一，需逐个模块改 JS，属内容层改动，本次未做。

## 2026-09-20 · 第 7 次 · 触屏去除粘滞 hover + 触摸目标达标
- 类型：修复 + 优化
- 做了什么：
  - **修复真实的触屏缺陷**：卡片/磁贴/按钮的 `:hover` 在手机上会"粘住"——点完之后位移与高亮一直残留到下一次点击前，看起来像卡在按压状态。新增 `@media (hover: none)` 块，中和卡片/磁贴的 hover 位移、底色与描边变化，以及按钮/分段控件/底栏/图标按钮/日历格子的 hover 变色，**保留 `:active` 的真按压反馈**。桌面鼠标设备（`hover: hover`）完全不受影响。
  - **触摸目标达标**：月历翻页按钮 30px → 34px。翻月是高频操作，30px 偏小易误触。
- 改动文件：`web/css/styles.css`（新增「触屏适配」媒体查询块；`.cal-nav button` 尺寸 30→34px）
- 验证：jsdom 回归 **19 项断言全 PASS、0 失败 0 报错**；CSS 完整性校验通过（括号 245/245）。
- 风险 / 遗留：
  - 该块必须与所有 `:hover` 规则**在其之后**才能生效（同优先级后者胜），因此放在「图标/插画/动画」段之前、所有组件规则之后。**后续新增组件若带 hover，无需改动本块**，除非该 hover 是位移类且需触屏抑制，则补进选择器列表。
  - `.btn-primary:hover { filter: brightness(1.04) }` 属滤镜而非位移/底色，触屏下保留（无害）。

## 2026-09-20 · 第 6 次 · 顶栏滚动玻璃化 + 刘海安全区
- 类型：修复 + 优化
- 做了什么：
  - **修复真实可用性缺陷**：顶栏始终是透明的（这是为首页天空让路的设计），但在各模块页滚动时，内容会从顶栏正下方直接穿过——文字与头像/主题按钮重叠，且没有任何遮罩。现在滚动超过 8px 后顶栏浮现磨砂玻璃底（`--glass-bg-strong` + `backdrop-filter` + 发丝下边 + 极淡投影），切页回到顶部时自动复位为透明；首页未滚动时天空照旧完整透出。
  - **刘海机型安全区**：顶栏补 `env(safe-area-inset-top)`，头像/主题按钮不再被状态栏压住。
  - **关键联动**：新增全局 `--sat: env(safe-area-inset-top, 0px)` 变量，顶栏内边距与首页天空的负 `margin-top` **共用同一变量**。二者必须同步——否则刘海机上顶栏变高、天空却没跟着上移，顶部会露出一条底色断痕。
- 改动文件：
  - `web/js/app.js`：新增 `syncHeader()`（读 `pageYOffset`，>8px 时给 `.app-header` 加 `is-scrolled`）；`init()` 注册 `scroll` 被动监听（`{passive:true}`，不影响滚动性能）；`showTab()` 末尾调用，切页回顶时复位。
  - `web/css/styles.css`：`--sat` 变量；`.app-header` 内边距含安全区 + 过渡；新增 `.app-header.is-scrolled`；`.home-sky` 负 margin 改为 `calc(-18px - 64px - var(--sat))`。
- 验证：jsdom 回归新增 3 条断言（顶栏存在 / 初始为透明态 / 派发 scroll 不报错），合计 **19 项全 PASS、0 失败 0 报错**；`node --check` 校验 `app.js` 语法通过；CSS 括号 245/245。
- 风险 / 遗留：
  - `--sat` 与首页天空的 `-64px` 基数是按当前顶栏高度（46px 头像 + 上下 10px 内边距）估算的。**若将来改动顶栏高度（如换头像尺寸、改内边距），必须同步改 `.home-sky` 里的 64**，否则天空与顶栏的衔接会错位。
  - jsdom 里 `window.pageYOffset` 恒为 0，无法模拟真实滚动，故只断言"不报错 + 初始透明态"；**真机滚动效果需在浏览器实际验证**。

## 2026-09-20 · 第 5 次 · 首页问候磨砂玻璃衬底（可读性修复）
- 类型：修复 + 优化
- 做了什么：首页问候语原本是纯白字直接压在程序化天空上；白昼天空偏浅，存在**对比度不足的可读性隐患**。改为磨砂玻璃衬底——半透明暗底 `rgba(38,30,52,.28)` + `backdrop-filter: blur(8px)` + 白色柔边框 + 加深文字阴影，保证清晨/正午/黄昏/夜间任何时刻文字都清晰，同时契合「玻璃暖房」语言。
- 改动文件：`web/css/styles.css`（仅 CSS，未动 JS）
- 验证：jsdom 回归 `bugcheck.js` **16 项断言全 PASS、0 失败 0 报错**；CSS 完整性校验通过（括号 233/233、无异常引号行）。
- 风险 / 遗留：衬底会略微遮住天空画面底部一小条。若不想要，删除 `.home-greet` 里的 `background` / `backdrop-filter` / `border` 三行即可还原。

## 2026-09-20 · 第 4 次 · 日历每日分色圆点 + 当日明细模块色
- 类型：优化（新增）
- 做了什么：「日期记录」页的日历原本每天只有一个紫色小点，看不出当天记了什么。改为**按当天涉及的模块渲染多个分色小圆点**（喂养雾蓝 / 睡眠藕紫 / 生长绿 / 经期玫瑰 / 孕期琥珀 / 宫缩珊瑚 / 记账紫），一眼看出这天记了哪几类；当日明细每条的图标也按所属模块着色，和圆点颜色一一对应，形成"日历→明细"的视觉呼应。颜色与首页模块磁贴**同源**，不引入第二套色。
- 改动文件：
  - `web/js/modules/calendar.js`：`.cal-cell` 内按当天模块的去重集合渲染 `.cal-dots`（最多 3 个 `<i class="cal-dot" data-accent="...">`）；当日明细 `<li>` 加 `data-accent`。
  - `web/css/styles.css`：分色板由 `.record-tile[data-accent=...]` **上提为全局 `[data-accent=...]`**（磁贴/圆点/明细图标共用单一色源）；新增 `.cal-dots` 容器 + 重写 `.cal-dot`；新增 `.day-list li[data-accent] .ico` 着色规则。
- 验证：jsdom 回归 `bugcheck.js` **16 项断言全 PASS、0 失败 0 报错**（原「今天格子有打点」断言仍成立）；CSS 完整性校验：括号 233/233、引号成对 0 异常、新类全部在位。
- 风险 / 遗留：
  - 一天超过 3 类模块时只显示前 3 个圆点（日期格子空间有限），明细里仍会全部列出。
  - 选中日（紫底）时圆点统一转白色，保证在紫底上的可读性。
  - **本次编辑过程中的教训**：曾因一次误替换把分色板首行损坏（留下未闭合的选择器），是靠 grep + 括号/引号完整性校验发现的，已修复。再次印证——**改完必须跑验证，不能凭"改完了"就认为没问题**。

## 2026-09-20 · 第 3 次 · 桌面端响应式放宽
- 类型：优化
- 做了什么：用户反馈宽屏下内容是被限在 640px 的"窄条"。新增 `@media (min-width:980px)` 断点，把 `.view` / `.app-header` / `.tabbar` 的 `max-width` 从 640px 放大到 860px；同时把「今日概览」`.stat-row` 从固定 2 列改为 `repeat(auto-fit, minmax(132px,1fr))` 自适应列（2/3/4 个指标都能整齐铺开）。移动端（<980px）完全不变。
- 改动文件：`web/css/styles.css`（仅 CSS，未动 JS）
- 验证：jsdom 回归 `bugcheck.js` **16 项断言全 PASS、0 失败 0 报错**；CSS 括号平衡。
- 风险 / 遗留：仅放宽到 860px；若还需更宽可继续上调该断点值。

## 2026-09-20 · 第 2 次 · 模块磁贴 hover 强调色光环
- 类型：优化
- 做了什么：在「玻璃暖房」基调上提升磁贴交互质感——给 `.record-tile:hover` 增加一层基于模块分色变量 `--tile` 的 1px 强调色描边光环（`box-shadow` 首段 `0 0 0 1px var(--tile-weak)`），悬停时玻璃磁贴浮出淡淡的分色描边，更"活"且不喧闹。配合已有的 `translateY(-1px)` 上浮。
- 改动文件：`web/css/styles.css`（仅 CSS，未动 JS）
- 验证：jsdom 回归 `bugcheck.js` **16 项断言全 PASS、0 失败 0 报错**。
- 风险 / 遗留：无。暗色模式 `--tile-weak` 已是半透明叠底，光环同样生效。

## 2026-09-20 · 第 1 次 · 编写交接报告与变更记录规范
- 类型：文档
- 做了什么：在仓库根目录新建 `交接报告.md`（项目简介、技术架构、设计系统「玻璃暖房」token 表、目录结构、运行方式、关键实现、已知问题、接手清单）与 `变更记录.md`（本文件），并确立"**每一步修改都必须写一条变更记录**"的强制规范（写入 `交接报告.md` 第五节）。本次的第 1~3 次记录即作为该规范的示范。
- 改动文件：新增 `交接报告.md`、`变更记录.md`
- 验证：人工核对文件可读、规范字段完整。
- 风险 / 遗留：无。

---

> 以下为历史里程碑的极简索引（完整演进见 `交接报告.md`）：
> - 设计系统 v2「玻璃暖房」确立（A+C 混合，重写 styles.css，零 JS 改动，16 项回归全过）
> - 首页去重：移除内嵌日历侧栏，日历独立为底部「日期记录」整页
> - 首页视觉升级：模块分色 + 「今日」角标 + 迷你统计卡
> - UI 交互基建：Util.confirm / toast / markInvalid / removeWithUndo
> - 首页重做：Web 版 swiftui-sky 天空 + 宝宝头像进设置
> - 早期：P0 对比度修正、CSS 双规则层合并、删除按钮图标化、favicon、PWA manifest
